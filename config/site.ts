export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "MySound - Explore New Music, Listen on the Go",
  description: "Dive into our vast library & discover soundzzz that match your taste",
};
